# Optional voice mode for the knowledge-worker (Mac Mini, VOICE_ENABLED=1).
# Native dependency: PortAudio (macOS: `brew install portaudio`) before sounddevice.
# Install on top of requirements.txt:  pip install -r requirements-voice.txt
openwakeword>=0.6.0
onnxruntime>=1.16
sounddevice>=0.4.6
numpy>=1.24
faster-whisper>=1.0.0
# Fullscreen answer pop-up (native Cocoa window; macOS only, SHOW_POPUP=1).
# Pinned to a release that ships prebuilt cp39 wheels so nothing is compiled
# (newer pyobjc has no Python 3.9 wheel and would need Xcode to build).
pyobjc-core==9.2; sys_platform == "darwin"
pyobjc-framework-Cocoa==9.2; sys_platform == "darwin"
