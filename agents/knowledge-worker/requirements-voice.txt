# Optional voice mode for the knowledge-worker (Mac Mini, VOICE_ENABLED=1).
# Native dependency: PortAudio (macOS: `brew install portaudio`) before sounddevice.
# Install on top of requirements.txt:  pip install -r requirements-voice.txt
openwakeword>=0.6.0
onnxruntime>=1.16
sounddevice>=0.4.6
numpy>=1.24
faster-whisper>=1.0.0
